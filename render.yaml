services:
  - type: web
    name: drwed03-5em1
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn 5em1:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1 --threads 8
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9